---
description: Чеклист деплоя — версия, коммит
alwaysApply: false
globs: 
---

# Деплой изменений

При каждом деплое (запрос «задеплой», «deploy»):

1. **Обнови версию** в `pyproject.toml` и `streamlit_app.py` (APP_VERSION)
2. Коммит и push

Порядок версий: 0.5.0 → 0.5.1 → 0.6.0 и т.д.
